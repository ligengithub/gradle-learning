group 'com.demo.gradle'
version '1.0-SNAPSHOT'

dependencies {
//    编译阶段
//    compile group: 'commons-collections'
//    测试阶段
}

// 发布jar文件
uploadArchives {
    repositories {
        flatDir {
            // 指定生成jar包的位置
            dirs 'repos'
        }
    }
}

